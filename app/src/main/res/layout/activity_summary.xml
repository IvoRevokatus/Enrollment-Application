<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

        <!-- TextView for displaying selected subjects -->
        <TextView
            android:id="@+id/selectedSubjectsTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Selected Subjects:"
            android:textSize="18sp"
            android:textStyle="bold"
            android:paddingBottom="8dp" />

        <!-- TextView for displaying total credits -->
        <TextView
            android:id="@+id/totalCreditsTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Total Credits:"
            android:textSize="18sp"
            android:textStyle="bold"
            android:paddingTop="8dp" />

</LinearLayout>
